# Copy this file to .env or export the variable in your shell.
# For local OpenAI-compatible servers such as LM Studio or llama.cpp, many servers accept any non-empty value.
OPENAI_API_KEY=replace-me
